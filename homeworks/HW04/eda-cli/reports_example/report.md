# EDA-отчёт

Исходный файл: `example.csv`

Строк: **36**, столбцов: **14**

## Качество данных (эвристики)

- Оценка качества: **0.72**

- Слишком мало строк: **True**

- Слишком много колонок: **False**

- Макс. доля пропусков по колонке: **5.56%**

- Доля дубликатов по строкам: **2.78%**

- Макс. доля нулевых значений по колонке: **66.67%**

- Слишком много пропусков: **False**

- Слишком много дубликатов: **False**

- Слишком много нулевых значений: **False**

## Заданные пороги для долей недопустимых данных

- Заданный порог для пропусков по колонке: **50.00%**

- Заданный порог для дубликатов по строкам: **20.00%**

- Заданный порог для нулевых значений по колонке: **90.00%**

## Колонки

См. файл `summary.csv`.

## Пропуски

См. файлы `missing.csv` и `missing_matrix.png`.

## Дубликаты

Количество дубликатов по строкам: 1.

См. файл `duplicates.csv`.

## Нулевые значения

См. файлы `zeros.csv` и `zeros_matrix.png`.

## Корреляция числовых признаков

См. `correlation.csv` и `correlation_heatmap.png`.

## Категориальные признаки

См. файлы в папке `top_categories/`.

## Гистограммы числовых колонок

См. файлы `hist_*.png`.
